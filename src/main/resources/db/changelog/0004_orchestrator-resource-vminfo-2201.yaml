databaseChangeLog:
- changeSet:
    id: 0004_orchestrator-resource-vminfo-2201
    author: m.perniola
    comment: >
      Add field vm_info to table resource to store virtual machine info as retrieved from IM
    changes:
    - sql:
        dbms: mysql
        sql: >
          ALTER TABLE `resource`
          ADD COLUMN `vm_info` TEXT NULL
